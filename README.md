# Simple Shopping Cart Counter 🛒

A beginner-friendly web project to understand the core concepts of JavaScript variables (`var`, `let`, and `const`) through a real-world shopping cart logic.

## 🚀 Features
- **Dynamic Updates:** Calculates item quantity and total price.
- **Tax Calculation:** Automatically applies a fixed GST/Tax rate (18%).
- **DOM Manipulation:** Updates the HTML content directly using JavaScript.

## 🛠️ Concepts Covered
- **`var`**: Used for the application name (Legacy global scope).
- **`let`**: Used for cart count and total price (Re-assignable values).
- **`const`**: Used for Tax Rate and final calculations (Fixed values).

## 📂 Project Structure
- `index.html`: The basic structure of the counter.
- `script.js`: The logic behind calculations and variable management.

## 📝 How to Use
1. Clone this repository:
   ```bash
   git clone https://github.com